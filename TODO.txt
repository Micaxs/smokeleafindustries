

- unidentified mixture bucket has THC/CDB values, this should be the same Levels: tooltip similarly to wehat is shows on weed items.
- the sequencer machine does not have any recipes (to make dna strands) to actually make the 25 strains, I want to make fileld dna strands to further process these in the sequencer to make a specific extract.
- putting an extract in the liquifier should give 250mb of oil.
- the cake and the brownie should also have the strain data (from the weed it is crafted with) when crafted and apply the effects when eaten.
- the input items in JEI recipe screens shows a grayscale item (seed, weed, bud, item) instead of the colored variants. it should not show the unidentified versions which are gray scale only show the colored one's with strain data.
- this also applies to crafting table recipes, joint and blunt recipes, the input weed items are all grayscaled and called unidentified weed, this should be the 25 strains and correctly colored.
- the liquifier jei screen should show each of the 25 strains (colored correctly) and their correct output oil (also colored correctly offcourse).
- the JEI tags, only show the unidentified weed/bud/etc. it should show the default 25 strains and correctly colored. It shouldn't show the unidentified versions at all.

